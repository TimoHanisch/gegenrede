# data/eval

Golden evaluation sets (`golden-de.jsonl`, `golden-en.jsonl`) and the eval runner land with M1.
The golden sets are human-curated; agents must never add, remove, or edit eval items (see `CLAUDE.md`).
